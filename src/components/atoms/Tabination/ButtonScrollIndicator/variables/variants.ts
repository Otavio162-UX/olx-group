import { TObjectVariant } from '@/types/global'
import { TButtonScrollIndicator } from '../ButtonScrollIndicator.interface'

export const buttonScrollVariants: TObjectVariant<TButtonScrollIndicator> = {
  left: 'left-0',
  right: 'right-0',
}
