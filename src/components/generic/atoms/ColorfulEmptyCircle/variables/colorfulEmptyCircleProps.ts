import { TBlogVariants, TObjectVariant } from '@/types/global'

const classNameVariants: TObjectVariant<TBlogVariants> = {
  immobile: 'bg-orange-90',
  autos: 'bg-green-90',
}

export const colorfulEmptyCircleProps = {
  classNameVariants,
}
